function solution(number){
    //Number.isInteger(23)
    console.log(number);
    
    for (let i = number; i < 4; i++) {
        console.log('Why');
    //   const check = number / number - 1;
      console.log(check)
    }
  }

solution(10);